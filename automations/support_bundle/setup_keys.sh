#!/usr/bin/env bash
# setup_keys.sh — placeholder
# SSH key setup is not used. Authentication is always password-based via sshpass.
# Credentials are collected interactively by ask_admin_creds / ask_root_creds.
echo "[INFO] SSH key setup is not used in this version."
echo "[INFO] Authentication is password-based via sshpass."
echo "[INFO] Run test_connections.sh to validate connectivity."
